# 契約04: ジューシー化ラウンド — AAA品質への引き上げ

委譲先: implementer-opus / ダイヤル: ゴール委譲（演出の創意はあなたの腕の見せ所。以下は方向性と品質バー）

## 目的
- 要求（ユーザー原文）: 「3D品質やシェーダーの改善、3D演出やエフェクト、UI演出やアニメーションやエフェクトをふんだんに活用しジューシーなプレイ体験を実現してトリプルAのゲーム品質に引き上げてください」
- ゴール: 機能的には完成しているゲームを、触っていて気持ちいい・見ていて豪華な「商用品質」の体験へ引き上げる。全アクションに視覚的な手応え（ジュース）がある状態
- 成功条件: 合格基準の全コマンド exit 0 + 演出カタログ（実装した演出の一覧と発火条件）の報告 + 性能ガード維持

## コンテキスト（これを読め。探すな）
- 開発引き継ぎ: `.claude/skills/seven-dev/SKILL.md`（必読 — アーキテクチャ・落とし穴・検証方法）
- 3D: `src/render/scene.ts`（カード配置・統一補間アニメ・カメラ）/ `cardTexture.ts` / `felt.ts`
- UI: `src/ui/app.ts`（扇形手札・D&D・ゾーン・ツモ演出）/ `styles.css`
- HEAD=af95514（R4完了: 操作系は完成済み。壊さないこと）

## 演出の方向性（各カテゴリから十分な数を。具体的な表現は委ねる）
1. **3D品質/シェーダー**: ライティングの豪華化（キーライト+リム+環境）/ フェルト・カードのマテリアル品質（MeshPhysicalMaterial の sheen/clearcoat 等）/ ソフトシャドウ / トーンマッピング調整 / ポストプロセス（three/examples の EffectComposer + UnrealBloomPass 等 — three 同梱物のみ、外部依存禁止）
2. **3D演出・エフェクト**: 配札のリズミカルな演出 / ツモ・捨て札のカード軌跡（トレイル/残像）/ メルド公開・ポン・チーの決め演出（パーティクル・光輪・カメラパンチ）/ 上がり・ラウンド終了のセレブレーション（紙吹雪パーティクル等）/ 手番プレイヤーのスポットライト / 山札が減る視覚表現
3. **UI演出・アニメーション**: 「ポン！」「チー！」「ロン/上がり！」等の大型コールアウト（麻雀ゲー風の勢いのある文字演出）/ 手番交代バナー / スコア票のカウントアップ / ボタンのマイクロインタラクション（hover/press）/ トースト・モーダルの入退場 / 鳴きウィンドウの緊張感（パルス・カウントダウン強調）
4. **（任意）プロシージャルSFX**: WebAudio 合成音（カード音・ポン効果音・勝利ジングル等）。外部音源ファイル禁止。実装する場合はミュートボタン必須

## 品質バー・エッジケース
- E1 性能: ミドルPCで60fps目標を維持。パーティクル・ポストプロセスはオブジェクトプール/上限数で制御。**モバイルでは自動で品質を落とす**（DPR・パス数・パーティクル数）
- E2 進行の非阻害: 演出はゲーム進行をブロックしない（スキップ可能 or 進行と並行）。通信対戦でスナップショットが演出中に届いても状態整合が壊れない（R4のドラッグ保留機構と同じ思想）
- E3 縦画面: すべての演出がモバイル縦でも成立（コールアウトの文字サイズ・パーティクル座標）
- E4 既存機能の非破壊: 手札D&D/フォーカス/ゾーン/鳴き/スコア票/ホットシート/通信対戦すべて現状の挙動を維持
- E5 プリモーテム: 「Bloomで白飛びして文字が読めない」→ 強度調整と文字系はポスト対象外レイヤに / 「低スペックでカクつく」→ 品質自動段階 / 「WebGLコンテキストロスト」→ dispose整合を維持
- （実装前にタスク固有の追加エッジケースを En+1.. として追補せよ）

## スコープと負の制約
- 変更してよい: `src/render/**`・`src/ui/**`・`src/main.ts`・`index.html`・`tests/`追加
- 変更禁止: `src/core/**` / `src/driver/**` / `src/net/**` / `docs/` / `.claude/**` / `scripts/**` / 新規npm依存（three同梱のexamplesモジュールは使用可）/ 外部アセットURL
- diff 上限: 3500行（lock除外）
- コミット禁止（メイン検証後にコミット）

## 環境ノート
- Node ポータブル版: bash で `export PATH="$PATH:/c/Users/kanet/tools/node-v24.19.0-win-x64"` 前置
- **seed タブは操作・ナビゲート禁止**（ユーザーがプレイ中の可能性）。検証は自前ポート＋新規タブで
- ブラウザペインは hidden で rAF 停止 → スクショ不可。DOM/JS駆動＋WebGL健全性検査で代替（SKILL.md参照）

## 合格基準（全て実行し、コマンドと exit code を報告に含めること）
- 実行シェル: bash（cwd=C:/Users/kanet/seven、PATH前置）
- `npm run build`
- `npm run typecheck`
- `npm test`（既存61件グリーン維持）
- `git diff --name-only -- src/core src/driver src/net | (! grep .)`（保護領域非改変 [ゲート]）
- 目視確認（DOM/JS代替可）: 1ラウンド完走で各演出の発火を確認し**演出カタログ**（演出名/発火条件/実装箇所）を報告 / 縦375pxでの成立 / コンソール・WebGLエラー0 / フレーム時間の簡易計測（演出多発時に16.7ms近傍を維持できているか）

## 出力契約
最終報告は日本語で40行以内。status / files_changed / commands_run(exit code) / 演出カタログ / 目視確認結果 / summary / notes（追補E・性能計測値）。**最終報告のstatus行が出るまでタスク完了と見なされない。**

## エスカレーション条件
- three同梱のexamplesモジュールで実現不能な要素があった（不足内容を報告）
- 性能目標と演出量が両立しない（トレードオフ案を添えて報告）
