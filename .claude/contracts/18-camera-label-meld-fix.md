# 契約18: 首振り調整・初期画角復元・ラベル/メルド配置修正5点

委譲先: implementer-opus / ダイヤル: 標準（修正対象特定済み）

## 目的
- 要求: ユーザー実プレイのフィードバック5点（下記）
- 成功条件: 合格基準の全コマンド exit 0 + 5項目の個別確認報告（status行必須）

## 実装項目（5点・すべて必須）

1. **スワイプの首振り方向を反転**: 現在の方向が直感と逆。ドラッグ/スワイプの向きと視線移動の対応を反転する（モバイルFPSの慣習=「画面を掴んで世界を動かす」感覚に合わせる）
2. **初期カメラを従来の見渡せる画角に**: 首振り化で目線が下がり(h1.85・席近接)卓全体が見づらい。**既定視点はR17以前の「卓全体が見える俯瞰寄りの構図」に戻し**、そこを基点に首振りできるようにする（目線位置=旧カメラ位置相当・注視=卓中央。ズームやピッチ制限も新基点で自然に）
3. **ネームプレートの画面端クランプを廃止**: 首振りで後ろを向くとラベルが画面端に張り付き巨大化して邪魔。クランプ処理を撤去し、視界外のラベルは自然に画面外へ見切れる/非表示になるようにする（背後にあるラベルがスクリーン投影で正面に出る「背面投影」の症状にも注意 — カメラ前方判定で非表示に）
4. **同一メルドの行分割禁止**: 4人プレイで奥のプレイヤーのメルドに4枚目を付けたら「K,A,2」と「3」が別の行に分かれた。**メルドは必ず1行にまとめ、収まらない場合はそのメルド（または席全体）のカードスケールを縮小して収める**（自席で既にやっているスケール適応の踏襲でよい。13枚シークエンスも1行+縮小で）
5. **メルドカードの基準サイズを縮小**: 初期カードサイズが大きく幅の余裕がない。基準スケールを一回り小さく（例: 0.85前後 — 視認性とのバランスで調整）し、行に余裕を持たせる

## エッジケース
- E1: 2〜6人×縦/横で初期画角が「全席・卓中央・自分のメルドゾーン」を収める / 視点リセット・ホットシート手番リセットも新画角へ
- E2: ラベルのカメラ背面判定（画面端張り付き・背面投影の両方が解消）/ 首振り中のラベルスケールが異常拡大しない
- E3: 13枚シークエンス1行+縮小の極端ケース / 付け札で行内に伸びる際の再レイアウトが滑らか
- E4: 方向反転がカメラ慣性・ピッチ制限・ズームと整合
- （実装前に追加エッジケースを追補せよ）

## スコープと負の制約
- 変更してよい: src/render/**, tests/**（meldLayoutテスト更新）
- 変更禁止: src/core/** src/driver/** src/net/** src/ui/**（レイアウト定数の参照変更が必要な場合のみ最小変更可・報告必須）/ docs/ .claude/ scripts/ / 新規npm依存
- diff 上限: 1200行 / コミット禁止 / seedタブ操作禁止

## 環境ノート
- Node PATH前置: `export PATH="$PATH:/c/Users/kanet/tools/node-v24.19.0-win-x64"`
- 必読: `.claude/skills/seven-dev/SKILL.md`

## 合格基準（全て実行し、コマンドと exit code を報告に含めること）
- `npm run build` / `npm run typecheck` / `npm test`（108件基準+更新分全グリーン）
- `git diff --name-only -- src/core src/driver src/net | (! grep .)`（保護領域ゲート）
- 5項目の個別確認（項目1は実ポインタでの方向検証・項目2は初期画角の投影確認・項目4はK,A,2+3の再現ケースが1行になる数値検証）

## 出力契約
最終報告は日本語35行以内。**status行必須**。status / files_changed / 5項目対応表 / commands_run / summary / notes。
